<h2>Edit order</h2>

<%= render "form.html", changeset: @changeset,
                        events: @events,
                        event: @event,
                        food_source_id: @order.food_source_id,
                        action: order_path(@conn, :update, @order) %>

<%= link "Back", to: order_path(@conn, :index) %>
